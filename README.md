# legacy-xls-export-with-jsp
Export some data in an excel through a JSP that calls a servlet (like in legacy apps)

install tomcat 10

mvn clean package the app

copy the war into the webapps folder of your tomcat 10 location folder

hit the url http://localhost:8080/export-1/

and click the button, an excel should be downloaded
